stages:
  - generate_uml
  - deploy_pages

generate_uml:
  stage: generate_uml
  cache:
    key: java-and-plantuml
    paths:
      - plantuml.jar
      - .java/
  before_script:
    - |
      # Используем Java из старого кэша
      if [ -d ".java/jdk-11.0.20+8" ]; then
        echo "✅ Используем Java из старого кэша"
        export JAVA_HOME=$(pwd)/.java/jdk-11.0.20+8
        export PATH=$JAVA_HOME/bin:$PATH
        java -version
      elif command -v java &> /dev/null; then
        echo "✅ Используем системную Java"
        java -version
      else
        echo "❌ Java не найдена. Java уже установлена в старом кэше, просто используйте её."
        echo "Попробуйте запустить пайплайн ещё раз - он найдет Java в кэше."
        exit 1
      fi
    - |
      if [ ! -f "plantuml.jar" ]; then
        echo "PlantUML не найден, загружаю..."
        wget -q https://github.com/plantuml/plantuml/releases/download/v1.2024.5/plantuml-1.2024.5.jar -O plantuml.jar || curl -L -o plantuml.jar https://github.com/plantuml/plantuml/releases/download/v1.2024.5/plantuml-1.2024.5.jar
      else
        echo "Используем PlantUML из кэша"
      fi
  script:
    - mkdir -p public
    - echo "<!DOCTYPE html><html><head><meta charset='UTF-8'><title>PlantUML Diagrams</title><style>body{font-family:Arial,sans-serif;margin:20px;background:#f5f5f5;}h1{color:#333;}a{display:block;margin:10px 0;color:#0066cc;text-decoration:none;padding:8px;background:white;border-radius:4px;}a:hover{background:#e8f4f8;}</style></head><body><h1>PlantUML Diagrams</h1>" > public/index.html
    - |
      for file in sequences/*.puml; do
        if [ -f "$file" ]; then
          filename=$(basename "$file" .puml)
          echo "Processing: $file"
          cd sequences
          java -jar ../plantuml.jar -tpng "$filename.puml"
          mv "$filename.png" ../public/
          cd ..
          echo "<a href='$filename.png' target='_blank'>📊 $filename</a>" >> public/index.html
        fi
      done
    - echo "</body></html>" >> public/index.html
    - ls -la public/
    - echo "=== ПУБЛИЧНЫЕ ССЫЛКИ НА ДИАГРАММЫ ==="
    - echo "После успешного выполнения пайплайна используйте ссылки:"
    - |
      for img in public/*.png; do
        if [ -f "$img" ]; then
          filename=$(basename "$img")
          echo "📊 $filename:"
          echo "   https://git.melonfashion.ru/befree/qa/befree-diagrams/-/jobs/latest/artifacts/file/public/$filename"
          echo ""
        fi
      done
  artifacts:
    paths:
      - public/
    expire_in: 30 days

pages:
  stage: deploy_pages
  dependencies:
    - generate_uml
  script:
    - echo "Деплой на GitLab Pages..."
    - ls -la public/
  artifacts:
    paths:
      - public